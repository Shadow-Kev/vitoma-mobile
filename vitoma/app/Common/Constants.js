/** @format */

'use strict'

import { Dimensions } from 'react-native'

const { width, height } = Dimensions.get('window')

export default {
  PostImage: {
    small: 'small',
    medium: 'medium',
    large: 'large',
    full: 'full'
  },
}
